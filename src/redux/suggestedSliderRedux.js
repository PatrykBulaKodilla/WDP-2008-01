/* Selectors */

export const getSuggestedSlider = ({ suggestedSlider }) => suggestedSlider;

/* Reducer */

export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
